[project]
name = "python-example"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "compass-api-sdk",
    "devtools",
    "dotenv",
    "web3",
    "ruff",
    ]
